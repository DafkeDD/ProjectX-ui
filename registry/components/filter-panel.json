{
  "name": "filter-panel",
  "title": "FilterPanel",
  "description": "Filterkolom met groepen, tellers, avatars, \\",
  "category": "Navigatie",
  "dependencies": [],
  "requires": [],
  "files": [
    {
      "path": "components/filter-panel.tsx",
      "content": "\"use client\";\nimport * as React from \"react\";\nimport { cn } from \"../lib/cn\";\nimport { Icon } from \"../icons/icon\";\nimport { useControllableState } from \"../lib/hooks\";\n\nexport interface FilterPanelProps extends Omit<React.HTMLAttributes<HTMLDivElement>, \"title\"> {\n  title?: React.ReactNode;\n  /** Aantal actieve filters; toont de wisknop zodra het meer dan nul is. */\n  activeCount?: number;\n  /** Wist alle filters. */\n  onClear?: () => void;\n  clearLabel?: string;\n  /** Donkere rail, zoals de ingeklapte Sidebar. */\n  tone?: \"surface\" | \"inverted\";\n}\n\nexport interface FilterGroupProps extends Omit<React.HTMLAttributes<HTMLDivElement>, \"onToggle\"> {\n  label: React.ReactNode;\n  /** Inklapbaar maken. */\n  collapsible?: boolean;\n  defaultOpen?: boolean;\n  open?: boolean;\n  onOpenChange?: (open: boolean) => void;\n  /** Toont zoveel opties en zet de rest achter \"toon meer\". */\n  maxVisible?: number;\n  moreLabel?: string;\n  lessLabel?: string;\n}\n\nexport interface FilterOptionProps\n  extends Omit<React.InputHTMLAttributes<HTMLInputElement>, \"type\" | \"onChange\"> {\n  label: React.ReactNode;\n  /** Vinkje of bolletje. */\n  type?: \"checkbox\" | \"radio\";\n  checked?: boolean;\n  /** Deels aangevinkt, bv. een groep met een deel van de kinderen aan. */\n  indeterminate?: boolean;\n  onCheckedChange?: (checked: boolean) => void;\n  /** Aantal resultaten rechts. */\n  count?: number;\n  /** Avatar of icoontje voor het label. */\n  lead?: React.ReactNode;\n}\n\n/** FilterPanel — kolom met filtergroepen, met een kop en een wisknop. */\nexport const FilterPanel = React.forwardRef<HTMLDivElement, FilterPanelProps>(function FilterPanel(\n  { title, activeCount = 0, onClear, clearLabel = \"Wissen\", tone = \"surface\", className, children, ...rest },\n  ref\n) {\n  return (\n    <div ref={ref} data-tone={tone} className={cn(\"pxui-filter\", className)} {...rest}>\n      {(title || (activeCount > 0 && onClear)) && (\n        <div className=\"pxui-filter-head\">\n          <span className=\"pxui-filter-title\">\n            {title}\n            {activeCount > 0 && <span className=\"pxui-filter-count\">{activeCount}</span>}\n          </span>\n          {activeCount > 0 && onClear && (\n            <button type=\"button\" className=\"pxui-filter-clear\" onClick={onClear}>\n              {clearLabel}\n            </button>\n          )}\n        </div>\n      )}\n      {children}\n    </div>\n  );\n});\n\n/** Eén groep filters, eventueel inklapbaar en met \"toon meer\". */\nexport const FilterGroup = React.forwardRef<HTMLDivElement, FilterGroupProps>(function FilterGroup(\n  {\n    label,\n    collapsible,\n    defaultOpen = true,\n    open,\n    onOpenChange,\n    maxVisible,\n    moreLabel = \"Toon meer\",\n    lessLabel = \"Toon minder\",\n    className,\n    children,\n    ...rest\n  },\n  ref\n) {\n  const [uitgeklapt, setUitgeklapt] = useControllableState<boolean>({\n    value: open,\n    defaultValue: defaultOpen,\n    onChange: onOpenChange,\n  });\n  const [alles, setAlles] = React.useState(false);\n\n  const items = React.Children.toArray(children);\n  const verborgen = maxVisible !== undefined && items.length > maxVisible && !alles;\n  const zichtbaar = verborgen ? items.slice(0, maxVisible) : items;\n\n  return (\n    <div ref={ref} data-open={uitgeklapt ? \"\" : undefined} className={cn(\"pxui-filter-group\", className)} {...rest}>\n      {collapsible ? (\n        <button\n          type=\"button\"\n          className=\"pxui-filter-group-label\"\n          aria-expanded={uitgeklapt}\n          onClick={() => setUitgeklapt(!uitgeklapt)}\n        >\n          {label}\n          <Icon name=\"chevronDown\" size={15} className=\"pxui-filter-chevron\" />\n        </button>\n      ) : (\n        <div className=\"pxui-filter-group-label\">{label}</div>\n      )}\n\n      {(!collapsible || uitgeklapt) && (\n        <div className=\"pxui-filter-items\">\n          {zichtbaar}\n          {maxVisible !== undefined && items.length > maxVisible && (\n            <button type=\"button\" className=\"pxui-filter-more\" onClick={() => setAlles((vorig) => !vorig)}>\n              {alles ? lessLabel : `${moreLabel} (${items.length - maxVisible})`}\n            </button>\n          )}\n        </div>\n      )}\n    </div>\n  );\n});\n\n/** Eén filteroptie: vinkje of bolletje, met tellertje en optioneel een avatar. */\nexport const FilterOption = React.forwardRef<HTMLInputElement, FilterOptionProps>(\n  function FilterOption(\n    { label, type = \"checkbox\", checked, indeterminate, onCheckedChange, count, lead, className, disabled, ...rest },\n    ref\n  ) {\n    const eigen = React.useRef<HTMLInputElement | null>(null);\n\n    React.useEffect(() => {\n      if (eigen.current) eigen.current.indeterminate = Boolean(indeterminate);\n    }, [indeterminate]);\n\n    return (\n      <label className={cn(\"pxui-filter-option\", className)} data-disabled={disabled ? \"\" : undefined}>\n        <input\n          ref={(node) => {\n            eigen.current = node;\n            if (typeof ref === \"function\") ref(node);\n            else if (ref) (ref as React.RefObject<HTMLInputElement | null>).current = node;\n          }}\n          type={type}\n          className=\"pxui-filter-input\"\n          checked={checked}\n          disabled={disabled}\n          onChange={(event) => onCheckedChange?.(event.target.checked)}\n          {...rest}\n        />\n        <span className=\"pxui-filter-box\" aria-hidden=\"true\">\n          <Icon name={indeterminate ? \"minus\" : \"check\"} size={12} strokeWidth={3} />\n        </span>\n        {lead && <span className=\"pxui-filter-lead\">{lead}</span>}\n        <span className=\"pxui-filter-label\">{label}</span>\n        {count !== undefined && <span className=\"pxui-filter-badge\">{count}</span>}\n      </label>\n    );\n  }\n);\n"
    },
    {
      "path": "components/filter-panel.css",
      "content": "/* ============================ FilterPanel ============================ */\n.pxui-filter {\n  display: flex; flex-direction: column; gap: 4px;\n  width: 100%; max-width: 280px; padding: calc(14px * var(--density)) 14px;\n  border: 1px solid var(--border); border-radius: var(--r-lg);\n  background: var(--surface);\n}\n.pxui-filter[data-tone=\"inverted\"] {\n  background: var(--sidebar-ink); border-color: var(--sidebar-ink-border); color: var(--sidebar-ink-text);\n}\n\n.pxui-filter-head { display: flex; align-items: center; gap: 8px; padding: 2px 6px 10px; }\n.pxui-filter-title {\n  display: inline-flex; align-items: center; gap: 8px;\n  font-size: var(--fs-md); font-weight: 650; color: var(--text); letter-spacing: -0.01em;\n}\n.pxui-filter[data-tone=\"inverted\"] .pxui-filter-title { color: var(--sidebar-ink-strong); }\n.pxui-filter-count {\n  display: inline-grid; place-items: center; min-width: 20px; height: 20px; padding: 0 6px;\n  border-radius: var(--r-full); background: var(--accent); color: var(--accent-fg);\n  font-size: 11.5px; font-weight: 700;\n}\n.pxui-filter-clear {\n  margin-left: auto; border: 0; background: none; padding: 4px 6px; border-radius: var(--r-xs);\n  font: inherit; font-size: var(--fs-sm); font-weight: 600; color: var(--accent); cursor: pointer;\n}\n.pxui-filter-clear:hover { background: var(--accent-tint); }\n.pxui-filter-clear:focus-visible { outline: none; box-shadow: var(--ring); }\n\n.pxui-filter-group { padding: 6px 0; border-top: 1px solid var(--border); }\n.pxui-filter-group:first-of-type { border-top: 0; }\n.pxui-filter[data-tone=\"inverted\"] .pxui-filter-group { border-color: var(--sidebar-ink-border); }\n.pxui-filter-group-label {\n  display: flex; align-items: center; gap: 8px; width: 100%;\n  padding: 7px 6px; border: 0; background: none; text-align: left;\n  font: inherit; font-size: 11.5px; font-weight: 650; letter-spacing: .08em; text-transform: uppercase;\n  color: var(--text-3); cursor: default;\n}\nbutton.pxui-filter-group-label { cursor: pointer; }\nbutton.pxui-filter-group-label:focus-visible { outline: none; box-shadow: var(--ring); border-radius: var(--r-xs); }\n.pxui-filter-chevron { margin-left: auto; transition: transform var(--dur) var(--ease); }\n.pxui-filter-group[data-open] .pxui-filter-chevron { transform: rotate(180deg); }\n.pxui-filter-items { display: flex; flex-direction: column; gap: 1px; }\n\n.pxui-filter-option {\n  display: flex; align-items: center; gap: 10px;\n  padding: calc(7px * var(--density)) 8px; border-radius: var(--r-sm);\n  font-size: var(--fs-base); color: var(--text); cursor: pointer;\n  transition: background var(--dur-fast) var(--ease);\n}\n.pxui-filter-option:hover { background: var(--surface-3); }\n.pxui-filter[data-tone=\"inverted\"] .pxui-filter-option { color: var(--sidebar-ink-text); }\n.pxui-filter[data-tone=\"inverted\"] .pxui-filter-option:hover { background: var(--sidebar-ink-hover); }\n.pxui-filter-option[data-disabled] { opacity: .5; cursor: not-allowed; }\n\n.pxui-filter-input { position: absolute; opacity: 0; width: 0; height: 0; }\n.pxui-filter-box {\n  display: grid; place-items: center; width: 17px; height: 17px; flex: none;\n  border: 1.5px solid var(--border-strong); border-radius: 5px; background: var(--surface);\n  color: transparent; transition: background var(--dur-fast) var(--ease), border-color var(--dur-fast) var(--ease);\n}\n.pxui-filter-input[type=\"radio\"] ~ .pxui-filter-box { border-radius: var(--r-full); }\n.pxui-filter-input:checked ~ .pxui-filter-box,\n.pxui-filter-input:indeterminate ~ .pxui-filter-box {\n  background: var(--accent); border-color: var(--accent); color: var(--accent-fg);\n}\n.pxui-filter-input:focus-visible ~ .pxui-filter-box { box-shadow: var(--ring); }\n.pxui-filter-lead { display: inline-flex; flex: none; }\n.pxui-filter-label { flex: 1; min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }\n.pxui-filter-badge {\n  flex: none; font-size: var(--fs-xs); color: var(--text-3); font-variant-numeric: tabular-nums;\n}\n"
    }
  ]
}
